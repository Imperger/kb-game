flags:
 backend:
   paths:
     - backend
 spawner:
   paths:
     - spawner
coverage:
   range: "60...90"
   status:
    project:                   
      default:                 
        enabled: yes           
        target: 90%
    patch:                   
      default:                 
        enabled: yes           
        target: 90%
codecov:
  disable_default_path_fixes: true
